---
description: Write comprehensive tests for a file
---

# Write Tests

1. **Identify Target**:
   - Ask the user which file needs tests (or use the active file).
2. **Generate**:
   - Run the `write-tests` skill on the target file.
   - Ensure it follows `testing-guidelines` (80% coverage, AAA pattern).
3. **Run**:
   - Execute the tests to verify they pass.
